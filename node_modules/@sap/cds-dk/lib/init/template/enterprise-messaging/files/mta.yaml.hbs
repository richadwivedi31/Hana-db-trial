---
modules:
 - name: {{cap.appName}}-srv
   type: {{cap.pLanguage}}
   requires:
    - name: {{cap.appName}}-messaging

resources:
  - name: {{cap.appName}}-messaging
    type: org.cloudfoundry.managed-service
    parameters:
      service: enterprise-messaging
      service-plan: default
      path: ./event-mesh.json
